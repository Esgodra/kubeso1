apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: gateway-net
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/gateway-net: "true"
  podSelector:
    matchLabels:
      io.kompose.network/gateway-net: "true"
